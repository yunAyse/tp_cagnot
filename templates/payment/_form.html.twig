<div class="container">
	<div class="section">
		<div class="row">
			<div class="col s12 center">
				<h4>Participez en quelques secondes</h4>
			</div>
		</div>
		<!--   Form Section   -->
		<div class="card">
			<div class="card-content">
				<div class="row">

					{{ form_start(form) }}
					{{ form_widget(form) }}
					<button class="btn btn-large waves-effect waves-light pink lighten-1 col s12">{{ button_label|default('Save') }}</button>
					{{ form_end(form) }}

				</div>
			</div>
		</div>
	</div>
</div>
